<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HostelMessMenuApp"
        tools:targetApi="31">
        <activity
            android:name=".Testing.DataByDateActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.CombinedDataActivity"
            android:exported="false" />
<!--        <activity-->
<!--            android:name=".Activity"-->
<!--            android:exported="false" />-->
        <activity
            android:name=".Activities.ShowDataActivity"
            android:exported="false"
            android:label="@string/title_activity_show_data"
            android:theme="@style/Theme.HostelMessMenuApp.NoActionBar" />
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <receiver android:name=".Notification.AlarmReceiver"/>
        
    </application>

</manifest>